use("twifs")
use("ramfs")

if configured("fs_iso9660"):
    use("iso9660")

if configured("fs_ext2"):
    use("ext2")

sources([
    "twimap.c",
    "pcache.c",
    "mount.c",
    "xattr.c",
    "vfs.c",
    "defaults.c",
    "path_walk.c",
    "fsm.c",
    "fs_export.c",
    "probe_boot.c"
])

